#include<iostream>
#include<cmath>
int main()
{
    double in,out;
    std::cout<<"Inserir numero: ";
    std::cin>>in;
    out=sqrt(in);
    std::cout<<"A raiz quadrada de "<<in <<" e:  "<<out;
    return 0;
}